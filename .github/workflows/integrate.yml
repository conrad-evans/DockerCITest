name: React Continuous Integration

on:
  pull_request:
    branches: [ main ]

jobs:
  test_pull_request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout/@v1
      - run: npm ci
      - run: npm test
      - run: npm build